def has_same_sign_neighbors(arr):
    for i in range(1, len(arr)):  
        if arr[i] * arr[i - 1] > 0:
            return "YES"
    return "NO"

if __name__ == "__main__":
    n = int(input())
    array = list(map(int, input().split()))
    print(has_same_sign_neighbors(array))
