def average(array):
    sum = 0
    for s in set(arr):
        sum+=s
    return sum/len(set(arr))

if __name__ == '__main__':
    n = int(input())
    arr = list(map(int, input().split()))
    result = average(arr)
    print(result)
